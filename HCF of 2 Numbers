#include<stdio.h>
int main(){
	int num1, num2, i;
	scanf("%d %d", &num1, &num2);
	
	
	if (num1 > num2){
	
	    for (i = num2; i>=1; i--){
		    if ((num2%i == 0) && (num1%i == 0)) break;}
		    printf("%d", i);}
		
	if (num2 > num1){
	
	    for (i = num1; i>=1; i--){
		    if ((num2%i == 0) && (num1%i == 0)) break;}
		    printf("%d", i);}
		
		
	
	return 0;
}
