#include <stdio.h>

//This Code Adds the all digits of a Number

int main() {
	int num, sum=0;
	printf("Enter any Number: ");
	scanf("%d", &num);
	
	while(num != 0){
		
		sum += num%10;
		num = num/10;
		
	}
	printf("The Sum of all digits is: %d", sum);
	
	
	return 0;
}
